# delete_history

Safely clear bash history — all of it or just the last N lines.

## Usage

```bash
runscript delete_history --all    # clear entire history
runscript delete_history 10       # remove last 10 lines
```
